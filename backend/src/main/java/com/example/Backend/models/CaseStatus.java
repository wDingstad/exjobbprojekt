package com.example.Backend.models;

public enum CaseStatus {

    NEW, // inte säker om jag vill ha new, kanske bara köra open istället.
    BLOCKED,
    FINISHED,
    OPEN,
    IN_PROGRESS,
    CANCELLED
}
